# Image size report (Phase 3, 2026-09-07)

Generated by `python3 scripts/optimize-images.py`. Source originals live in
`assets/img/src/` (git-tracked, not deployed to the browser directly).
Outputs are `assets/img/<name>.webp` (primary, served via `<picture><source>`)
and `assets/img/<name>.png` (fallback for browsers without WebP support).

| name | width×height | original KB | webp KB | png fallback KB |
|---|---|---|---|---|
| opencl-result | 875×436 | 322 (src png) | 25.1 | 316.4 |
| rowscope-plot | 1600×859 (from 1627×874) | 104 (src png) | 43.8 | 210.2 |

Budget check (06-assets-seo-performance.md §4, "증거 이미지 < 150KB 각"):
- WebP (primary served format via `<picture><source type="image/webp">`): both well under 150KB.
- PNG fallback exceeds 150KB for both files (only used by browsers without WebP support,
  effectively none in 2026). No further tool available to shrink PNG (no pngquant/ImageMagick
  in this environment); Pillow `optimize=True` is already applied. Not treating this as a
  budget violation since the primary served asset (WebP) meets the target — flagging for
  planner awareness only.

`<picture>` markup for implementer:

```html
<picture>
  <source type="image/webp" srcset="assets/img/opencl-result.webp">
  <img src="assets/img/opencl-result.png" width="875" height="436"
       loading="lazy" decoding="async" alt="…">
</picture>

<picture>
  <source type="image/webp" srcset="assets/img/rowscope-plot.webp">
  <img src="assets/img/rowscope-plot.png" width="1600" height="859"
       loading="lazy" decoding="async" alt="…">
</picture>
```
